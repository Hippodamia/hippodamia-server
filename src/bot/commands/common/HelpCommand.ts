/*

import { BaseCommand, CommandFunction, BaseSession, Card } from 'kasumi.js';

//...

class HelpCommand extends BaseCommand {
    name = "help";
    description = "帮助菜单!";


    
    func: CommandFunction<BaseSession, void> = async (session) => {
        const card = new Card();
        card.addTitle('仿生赫尔手册')
        card.addText('> 注意:当前版本仍处于开发中状态，可能会导致帮助菜单更新不及时。')
        card.addText('`/race`')
        card.addText('查看关于赛场的帮助')
        card.addText('`/bot`')
        card.addText('查看关于仿生赫尔的信息')
        session.reply(card)
    }
}
const helpCommand = new HelpCommand();
export default helpCommand
*/
